var links = document.getElementsByClassName('product-links'), len = links.length;

var load_form = document.getElementById('add-product-form').innerHTML = '<%= j(render :template => "products/package_item_form.html") %>'; 

for (var i = 0; i < len; i ++) {
  links[i].addEventListener('click', load_form);
}

